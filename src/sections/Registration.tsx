import React from 'react';
import { RouteComponentProps} from "@reach/router"

export const Registration = (props: RouteComponentProps) => {
  return (
    <div className="App">
      <div>Registration Page</div>
    </div>
  );
}

